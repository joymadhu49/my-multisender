# Reown / WalletConnect Cloud project ID.
# Required to initialize the wallet-connect modal (Reown AppKit).
#
# 1. Create a free project at https://dashboard.reown.com
# 2. Copy this file to .env  ->  cp .env.example .env
# 3. Paste your project ID below.
#
# NOTE: Vite only exposes env vars prefixed with VITE_ to client code
# (import.meta.env.VITE_WALLETCONNECT_PROJECT_ID).
VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Deployer wallet for hardhat deploys (0x-prefixed private key).
# Used by hardhat.config.cjs for all networks. NEVER commit the real key.
PRIVATE_KEY=

# Optional: Etherscan V2 API key (works across chains, incl. opBNB)
# for `npx hardhat verify`. Get one at https://etherscan.io/apis
ETHERSCAN_API_KEY=

# Optional RPC overrides (defaults are the public endpoints in
# hardhat.config.cjs). Robinhood Chain is an Arbitrum Nitro L2 with native ETH.
# ROBINHOOD_RPC=https://rpc.mainnet.chain.robinhood.com
# ROBINHOOD_TESTNET_RPC=https://rpc.testnet.chain.robinhood.com/rpc
